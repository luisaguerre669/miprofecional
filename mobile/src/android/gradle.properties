# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx10248m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Version of flipper SDK to use with React Native
FLIPPER_VERSION=0.125.0

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86,x86_64,arm64-v8a,armeabi-v7a
# If you need Android NDK r23b, you can download it from here:
# https://dl.google.com/android/repository/android-ndk-r23b-darwin.zip
# Then set the path in ndk.dir property below.
# ndk.dir=/Users/your-username/Library/Android/sdk/ndk/23.1.7779620

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in React Native.
# See https://reactnative.dev/docs/new-architecture-enabled for instructions on how to enable it.
newArchEnabled=false

# Use this property to enable support to the Hermes JavaScript engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native
expo.gif.enabled=true

# Enable webp support in React Native
expo.webp.enabled=true

# Enable network inspector
org.gradle.jvmargs=-XX:MaxMetaspaceSize=512m --add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED --add-exports=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED --add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED

# Suppress unsupported compileSdk warning
android.suppressUnsupportedCompileSdk=34

# Force Java 11 compatibility
android.useAndroidX=true
android.enableJetifier=true
